---
description: WatchIO live parameter updates — monitor diff, workspace restore, no regression
globs: frontend/src/hooks/useWatchIo.ts,frontend/src/utils/pinnedVariables.ts,frontend/src/utils/watchIoLiveMonitor.ts,frontend/src/stores/variableStore.ts,frontend/src/utils/workspacePersistence.ts,frontend/src/hooks/useWorkspacePersistence.ts,frontend/src/stores/plotStore.ts
alwaysApply: false
---

# WatchIO live parameter updates

Applies when editing pinned parameters, workspace restore, `useWatchIo`, or variable/plot stores.

## Symptom to protect against

After **page refresh**: Parameters / Plot show **frozen** values; **description empty** (no varleaves metadata).

## Architecture (do not regress)

| Layer | Responsibility |
|-------|----------------|
| `runPinnedLivePipeline` | **Only** orchestrator for pinned live recovery |
| `fetchVarLeaves` (branch, priority) | Pinned metadata — **not** `varinfo` (STOMP often ignores it) |
| `syncWatchIoMonitorDiff` | **add/delete only** — never `type:list` on WebSocket (resets update stream) |
| `serverMetadataLoaded` | Set by `buildWatchIoVariable` (varleaves) or workspace `pinnedMetadata` cache |

## WatchIO transport (`watchIoHttp` / `watchIoWs`)

- **Metadata**: priority `varleaves` per missing pinned branch; workspace may cache `dataType`/`description` for instant monitor add.
- **Monitor**: `addVariable(name, mode, dataType)` / `removeVariable` via diff only.
- **Do not** call `add` + `list` together; do not reintroduce `setMonitorList`.
- **`requestUpdate` interval** starts only after `serverMonitored` is non-empty.
- **Do not** add parallel effects that sync monitor or fetch pinned metadata outside the pipeline.

## Workspace restore

- `seedPinnedVariableCache` → values: `sessionCacheOnly: true`; metadata cache → `serverMetadataLoaded: true` when `dataType` known.
- `isPinnedVariableLiveLoaded` = `serverMetadataLoaded === true`.
- `applyUpdate` must not clear `sessionCacheOnly` until metadata loaded.

## Plot after restore

- `resyncPlotSeries` on first live `update` or varleaves for plot vars.
- Plot vars: monitor mode **`value`**; table params: **`set`**.

## After changing this area

1. Select param → values tick live.
2. Hard refresh ×10 → description present, values keep ticking.
3. Message log: `send varleaves` → `recv varleaves` → `send add` → steady `recv update`; no repeated `list`.
